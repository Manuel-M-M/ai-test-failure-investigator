{
  "name": "ai-test-failure-investigator",
  "private": true,
  "version": "0.1.0",
  "description": "AI-powered developer tool to investigate test failures and suggest fixes.",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:api": "pnpm --filter api dev",
    "build": "pnpm -r build"
  }
}